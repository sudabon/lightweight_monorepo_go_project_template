# Repository

DB access implementations belong here once the template grows real tables.

Keep SQL and driver-specific code out of handlers. Services should call
repositories when persistence is required.
